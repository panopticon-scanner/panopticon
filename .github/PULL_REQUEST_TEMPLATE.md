<!-- Title: conventional prefix (fix/feat/test/docs/ci) plus the issue number, e.g. "fix(discovery): ... (#1234)". -->

**What this changes and why**

<!-- Two or three sentences. Link the issue with "Closes #N". If a reviewer or a self-scan finding drove it, name the catalog code. -->

**How it was verified**

<!-- Commands and results. A red-then-green test run is the expected shape for a fix. -->

**Checklist**

- [ ] Tests added or updated; any new test file is claimed in the root `panopticon.yml` (`tests/test_matrix_coverage.py` fails otherwise)
- [ ] No test invokes a real host CLI, Docker, or a scanner binary; adapters are exercised through their injected runners
- [ ] `python3 -m ruff check skill/scripts/ tests/` and `python3 -m pytest tests/ -q` pass locally
- [ ] Scanner launch changes (cwd, argv, `-w`) were tried once through the real tools image
- [ ] Docs updated where behaviour changed (`docs/PANOPTICON.md`, `README.md`, `DEVELOPMENT.md`)
- [ ] If any part of this PR was generated by an AI tool, the PR body says which parts
