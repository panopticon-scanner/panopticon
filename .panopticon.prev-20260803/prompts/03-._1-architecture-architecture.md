You are the `architecture` lens sweep for panopticon panel `architecture` in group `._1`.
Files: CHANGELOG.md, CODEOWNERS, CONTRIBUTING.md, CONTRIBUTORS.md, DEVELOPMENT.md, Dockerfile, Dockerfile.fixtures, LICENSE, README.md, pyproject.toml, .github/workflows/ci.yml, .github/workflows/codeql.yml, .github/workflows/docker-publish.yml, .github/workflows/security.yml, docs/superpowers/2026-08-03-calibration-fixmes.md
Security mode: standard
Depth: shallow

## Your task

Perform a narrow, mechanical review of the listed files **only through the `architecture` lens**.
Return ONLY a raw JSON object `{"findings": [...]}` as your final message — you cannot write files; the orchestrator writes your findings to `.panopticon/findings-._1-architecture-lens_sweep-architecture.json`.

## Rules

- Findings must cite a rule, pattern, or line of code. Uncited claims are not allowed.
- Keep descriptions short and factual.
- Do not write narrative or general advice.
- Do not perform GitHub writes, repo mutations, or credential mints.

## Finding format

- id: ^[A-Z]{2,8}-\d{3,}$
- severity: CRITICAL|HIGH|MEDIUM|LOW|INFO
- panel: "architecture"
- lens: "architecture"
- category: "architecture"
- location: {file, line_start[, line_end, function]}
- title, description, impact, remediation, references[]
- source_role: "lens_sweep"
- depth: "shallow"

Each finding MUST include a `provenance` object:

```json
"provenance": {
  "discovered_by": "agent:lens_sweep",
  "expanded_by": null,
  "confirmed_by": null,
  "model": "<model-name>",
  "model_version": "<version>",
  "confirmation_status": "UNVERIFIED",
  "confirmation_reasoning": null
}
```

And a `citations` object with at least one of:
- `cwe`: list of CWE IDs (e.g., `["CWE-89"]`)
- `owasp`: list of OWASP Top 10 categories (e.g., `["A03:2021"]`)
- `cve`: list of CVE IDs (if applicable)

## Tool policy

Your only tools are Read, Grep, Glob. You must not use Bash, Edit, Write, Agent under any circumstances.


## Orchestrator note

You cannot write files. RETURN the raw JSON object {"findings": [...]} as your final message; the orchestrator writes it to .panopticon/findings-._1-architecture-lens_sweep-architecture.json.
